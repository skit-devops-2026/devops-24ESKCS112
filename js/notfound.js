/* ============================================================
   TravelX - notfound.js
   The 404 page has no page-specific interactivity beyond what
   common.js already wires up (navbar, dark mode, etc.), so this
   file is intentionally empty. It exists so every page still has
   its own JS file to extend later if needed.
   ============================================================ */
